package session_service

//every seesion Uuid == every device instance
//an every device can have multi users
//so an uuid -> to multi users
//uuid is per instaltion base
//every push should_service push to mutip user on one device
//so in push: every user can have multi ws and each ws is for mutlti users


//todo change to cache package instead of manulay doin it