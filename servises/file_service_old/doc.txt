TODO:
-for higher resloution like 1080 server originals if original is lower than 1080
-check to if support for _small _large ...
-righ now any extenstion we use like: .webp or .none will produce a file on disk and store request, check if we could server without the exten like: storing on disk wihtout the exten or some defulte exten like: .msfile (this way we have both extena and for every type.
	or just store db extention after loading from db
-add support for session checking and rate limit
-extention limits cheking and how to support all other msg files
-check if we need to class for requst handling one for images with resizing and other for files raw serving
-check gif support and its mp4 and resizeing
-support for multi disk

